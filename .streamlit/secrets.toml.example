# Optional but recommended for reliable live 2026 season betting lines.
# Create a free key at SportWizzard, then copy this value into
# Streamlit Community Cloud -> App settings -> Secrets.
SPORTWIZZARD_API_KEY = "sw_live_your_api_key_here"
