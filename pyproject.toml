[project]
name = "fantasy-football-historical-lab"
version = "0.1.0"
description = "Historical fantasy football threshold and positional analytics dashboard."
readme = "README.md"
requires-python = ">=3.12,<3.14"
dependencies = [
    "streamlit>=1.38,<2",
    "pandas>=2.2",
    "numpy>=1.26",
    "plotly>=5.24",
    "requests>=2.32",
    "pyarrow>=17.0",
]

[tool.uv]
package = false
